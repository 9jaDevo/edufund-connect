import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Toaster } from 'react-hot-toast'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'EduFund Connect - Transparent Education Funding',
  description: 'Empowering transparent sponsorship for students and community projects with verified monitoring',
  keywords: ['education', 'funding', 'sponsorship', 'transparency', 'NGO', 'students'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Toaster position="top-right" />
      </body>
    </html>
  )
}
